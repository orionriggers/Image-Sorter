# --- File generati dal programma a runtime, mai da tracciare ---
image_sorter_config.json
image_sorter_config.json.bak
image_sorter_error.log
image_sorter_history.json
image_sorter_metadata.json
image_sorter_dupscan_cache.json
backup_auto/
.deck_focus.json
.deck_inbox_*.json
.deck_owner.lock
__pycache__/
*.pyc

# --- Cartelle di prova / materiale personale, non fanno parte del programma ---
foto natura/
foto prova/
prova/
WhatsAppsg/
backup/
idee/
lavori/
old versions/
paranchi/
sf_icone/

# Backup completi del progetto fatti a mano prima di modifiche (es.
# "1.41/", "1.42/"...) — un numero di versione come nome di cartella
# alla radice, invece di elencarle una per una ad ogni nuova versione.
/[0-9].[0-9][0-9]/

# Manuale HTML con screenshot incorporati — generato, pesante, per ora
# escluso (si può rigenerare quando serve). Da rivedere se si decide di
# distribuirlo insieme al programma.
manuals/
ToDoSorter.txt
